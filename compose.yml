services:
  app:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    container_name: clone-tabnews-node-18
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      HOME: /tmp
      npm_config_cache: /tmp/.npm
    stdin_open: true
    tty: true
